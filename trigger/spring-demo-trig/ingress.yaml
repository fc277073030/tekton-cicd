apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: el-spring-demo-listener
spec:
  rules:
    - host: el-spring-demo-listener.oifipaas.com
      http:
        paths:
          - path: /
            backend:
              serviceName: el-spring-demo-listener
              servicePort: 8080
